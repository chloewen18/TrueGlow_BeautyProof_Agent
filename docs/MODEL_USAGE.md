# 对外模型使用声明

TrueGlow提供内容风险辅助证据，不证明创作者意图，不认证产品功效，不识别人脸身份。

| 模块 | 实际实现 | 输出及边界 |
|---|---|---|
| Main Agent | 确定性规划、融合、分级与模板解释 | 默认未调用在线大模型；支持的LLM扩展不计入当前实测 |
| 来源 | ExifRead + SHA256 | 元数据缺失不证明伪造；不验证C2PA |
| 通用取证 | 官方TruFor网络 ae54475 + 成员二交付权重 | CPU推理；异常定位与置信度；可疑分不经美妆域概率校准；最长边默认1024，缩小会在输出标记 |
| 修饰 | 成员三ResNet18 Stage1/Stage2 | 通用修饰及四类操作信号；域外未经校准，不代表修图意图 |
| 前后条件 | 成员三Stage3条件回归 + ORB/RANSAC差异图 | 不是产品因果；差异图不是操作级真值；同图控制不代表不同帧负样本 |
| OCR | PaddleOCR / PP-OCRv5 mobile | CPU本地识别，可出错；用户可修正文案 |
| 文本 | 成员四词典与手写语义规则 | 类名MockSemanticLayer仍是确定性规则，不是远程语义大模型；按输入真实计算，与预设演示信号不同 |
| 功效 | 五产品交付品牌资料库v2 | 精确产品匹配；保留来源、等级、局限；本次未在线复核品牌页面 |

演示案例明确标记mock；运行失败不回落为模拟结果。原始输出及模型哈希随报告留存。

TruFor保留原作者许可及署名（`vendor/trufor/LICENSE.txt`、`LICENSE_CMX.txt`），源码包含非营利用途限制，不应宣称本项目已获商业授权。成员三权重的训练数据许可与后续使用范围仍需原交付者确认。FFHQ/FFHQR数据沿用原始元数据和许可说明，不重新授权其人像。

志愿者图片默认仅在本机内部评测，不训练、不公开、不上传外部模型服务。
