# TrueGlow 映真 · 状态同步与下一步（含可直接发群的话术）

> 整理时间：**2026-09-10 晚**｜ 距 10-10 交稿约 **30 天**
> 详细分工见同目录 `五人分工与行动清单.md`（唯一有效版本，v1–v4 已作废）
> 仓库：`git@github.com:chloewen18/TrueGlow_BeautyProof_Agent.git`（已同步，commit `19e729f` + `52562ac`）

---

## 一、当前真实状态（一页看懂）

| 模块 | 负责人 | 状态 | 一句话 |
|---|---|---|---|
| Agent 调度 / 风险分级 / 报告 | 成员 1 | 🟢 真可跑 | 最值钱的部分，规则扎实 |
| 来源溯源 EXIF | 成员 2 | 🟢 真解析 | OK |
| 来源溯源 C2PA | 成员 2 | 🟡 三态规整 OK，验证未接 | 已能区分 absent/not_verified/unsupported/error |
| 通用图像取证 TruFor | 成员 2 | 🔴 权重刚重新拉取 | 旧交付物是假数据；官方权重上次下载损坏，正在重下 |
| Before/After 一致性 | 成员 3 | 🔴 仍是假数据 | 方向 Bug 已修，算法没做实 |
| 磨皮 / 纹理检测 | 成员 3 | 🔴 仍是假数据 | 未做 |
| 妆效归因规则 | 成员 3 | 🔴 未做 | 胜负手 |
| 配对数据集 | 成员 3 | 🟢 100 对已交付 | 质量很好 |
| OCR / 宣称提取 / 功效证据 | 成员 4 | 🟢 真跑通 | 全队完成度最高 |
| 前端页面 | 成员 5 | 🟢 五分区完成 | OK |
| 评测页数字 | 成员 5 | 🔴 手写占位已清空 | 待真实数字回填 |

**一句话总结：流程、文案、数据、界面都有；最核心的「看图」能力（取证 + 前后对比归因）还是假的。本轮唯一目标就是把这两块从假变真。**

---

## 二、五人分工速览

> 完整版见 `五人分工与行动清单.md`。这里是速览。

**成员 1（你，Main Agent / 系统架构）**
- 已有：全链路 Agent、5 个 Tool 接口、报告结构、风险分级规则。
- 接下来：**9/13 前冻结接口 v2**（加 `mode` / `engine` / `provenance` 三字段）；做 Tool 适配器框架（每个模块允许「真实版 / 假数据版」，用哪个必须写进输出）；分数方向护栏 `integrity_score = 1 - trufor_score`；接成员 3 归因规则；端到端 3 个演示案例；汇总对外模型使用声明。
- 要改：之前 `before_after` 没输入却断言「归因 Strong」已修为 `Unknown`；报告里要能看出每条证据是真算还是写死。

**成员 2（来源溯源 / 通用取证）**
- 已有：EXIF 真解析；官方 TruFor 代码到位。
- 接下来：用官方权重跑通推理（定位图 / 置信度图 / 评分三件套）；C2PA 接 `c2pa-python`；分数方向转换；困难负样本 + 误报率报告。
- 别做：不用 Mirror-of-Truth、不自己从零训练、总投入压在 3 天内。
- 三坑：`test.py` 里 `except: pass` 会吞报错（先改成 `raise`）；CPU 必须 `-g -1`；输出是 `.npz` 不是图片（用 `visualize.py`）。

**成员 3（底妆修饰 / 妆效归因 ★ 胜负手）**
- 已有：100 对配对数据集（质量高）。
- 接下来：Before/After 用**经典 CV** 做实（MediaPipe 对齐 + 亮度/色温/高频能量比，不用训练）；皮肤平滑/纹理/曝光/白平衡检测；用 100 对验证；输出归因等级 Low/Medium/High/Unknown（绝不写「产品贡献 X%」）。
- 关键：经典算法不用训练，100 对全部可当评测集，直接产出可对外说的检出率/误报率。
- 大坑：数据里 `ground_truth_strength` 全是占位的 `medium`，**绝不能当真值训练**。

**成员 4（文本 / 评论 / 功效证据）**
- 已有：OCR、宣称词典、5 个产品功效数据、盲测 20/30。
- 接下来：补齐 `full_pipeline_v1.py` 缺的依赖文件让它独立跑；盲测缺口补词（目标 ≥27/30）；解释模板 5 套；功效数据加「证据等级 + 官方来源 + 局限」。
- 别做：AI 文本检测别当定罪工具。

**成员 5（前端 / 数据集 / 评测）**
- 已有：Streamlit 五分区前端。
- 接下来：**评测数字治理**（只放真实/目标值，不放无来源数字）；前端加「真实 / 假数据」徽章；启动志愿者真实数据采集（W2 必须动）；评测可视化只上真实数字。
- 评委最容易抓把柄的就是评测页，宁可空着写「待测」也别放没来源的数字。

---

## 三、成员 1（你）接下来要做的事 —— 优先级清单

| 优先级 | 任务 | 截止 | 说明 |
|---|---|---|---|
| 🔴 P0 | **冻结接口 v2**：加 `mode` / `engine` / `provenance` 三字段 | **9/13 硬线** | 你握着全员节奏，每延一天成员 5 就多空转一天 |
| 🔴 P0 | 定 Tool 适配器规范：每个模块「真实版 / 假数据版」双实现，用哪个写进输出 | 本周 | 本轮最值钱的工程资产 |
| 🟠 P1 | 分数方向护栏：`integrity_score = 1 - trufor_score` 落到 schema | 本周 | 不转，结论整体判反 |
| 🟠 P1 | 把成员 3 的归因规则接进报告 | W2 | 接完才有「妆效归因」这个差异化卖点 |
| 🟡 P2 | 端到端 3 个演示案例（原图 / 轻修 / 重P 各一） | W3 | 答辩直接用 |
| 🟡 P2 | **汇总对外模型使用声明**：用了哪些第三方模型、什么许可、我们自己的贡献 | W2 | 评委必问，建议你牵头写 |

> 说明：之前标注的「修 grader 方向问题」**已完成**（无输入 → `Unknown`），本次不再占用你的队列。

---

## 四、可直接发群的消息（复制粘贴即可）

```
各位，我把当前最基础、最紧急的状态理了一遍，同步一下，大家接着往下推。

【已搞定】
1) Before/After 方向 Bug 已修：之前没输入时默认返回"全部一致+归因 Strong"（等于一行没算就说是产品功劳），跟我们"证据不足就 Unknown"的原则相反。现已改成无信号→Unknown，有真实差异才判 Weak/Strong。两仓库都已改并跑通。
2) 评测数字治理：benchmark 里那些 F1=0.78 之类的手写数字已清空，改标"目标值/待测"，明确无来源。评委一问就穿的假数字不会再上页面。
3) C2PA 三态规整：之前一律返回 error，现已区分 absent（无签名）/ not_verified（疑似有但无验证库）/ unsupported_format（格式不支持）/ error（读取异常）。

【TruFor 权重说明】
之前那次权重下载包损坏了（MD5 对不上），已删除重新拉取，现已下完并解压好（MD5 校验通过）。成员 2 直接用官方权重跑推理，不用自己训：
  python test.py -in 图片或目录 -out 输出目录 -exp trufor_ph3 TEST.MODEL_FILE "pretrained_models/weights/trufor.pth.tar"
两个坑：① test.py 里 except:pass 会吞报错，先改成 raise；② 没 GPU 加 -g -1。
⚠️ TruFor 的 score 是"越高越可疑"，接进我们系统时务必转成 integrity_score = 1 - score（我们字段是"越高越完整"，不转结论会整体判反）。

【接下来各自接手】
- 成员2：权重下完→用官方权重重做 TruFor 取证；再做困难负样本误报率报告。
- 成员3：把100对 FFHQ/FFHQR 数据集用起来——用经典CV（MediaPipe对齐+亮度/色温/高频能量比）把 before/after 从 mock 做实。经典方法不用训练，100对全部当评测集，直接出可对外说的检出率/误报率。⚠️数据里 ground_truth_strength 全是占位的 medium，别拿它当真值。
- 成员4：补齐 full_pipeline_v1.py 缺的依赖文件（claim_extractor_mock_v2_1.py 没随包上传，现在跑不起来），让整条流水线能独立运行。
- 成员5：①启动志愿者真实数据采集（W2必须动，否则W3拿不出可信评测）；②评测页只放真实/目标值，别再放无来源数字。
- 我（成员1）：9/13 冻结接口 v2（加 mode/engine/provenance 三字段），随后做证据融合与报告生成。

【时间线】
9/13 接口冻结 → 9/30 内部演示冻结 → 10/1–10/10 只做 PPT/视频/文档/演练，不写新功能。

两件事需拍板：①志愿者招募谁牵头（建议成员5）；②对外模型使用声明谁汇总（建议我来：用了哪些第三方模型、什么许可、我们自己的贡献是什么）。

详细分工见仓库 docs/progress/五人分工与行动清单.md（v1–v4 已作废，以此为准）。
```

---

## 五、还需拍板的两件事

1. **志愿者招募谁牵头？** 建议成员 5 牵头、全员转发，W2 必须启动，否则 W3 拿不出可信评测。
2. **对外模型声明由谁写？** 建议成员 1（你）汇总：用了哪些第三方模型、分别什么许可、我们自己的贡献是什么。
