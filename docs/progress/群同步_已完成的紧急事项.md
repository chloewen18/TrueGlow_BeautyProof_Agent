# TrueGlow 进度同步（可直接发群）

各位，我先把手上最基础、最紧急的几件事做了，同步一下，大家接着往下推。

## 一、已经搞定（无需再管）

**1. 成员 2 的 TruFor 卡点已解除**
官方权重正在下载到 `TruFor_train_test/pretrained_models/`（261MB，后台跑着，稍后提醒你验证）。
下完后成员 2 直接用官方权重跑推理即可，不用自己训：
```bash
python test.py -in 图片或目录 -out 输出目录 \
  -exp trufor_ph3 TEST.MODEL_FILE "pretrained_models/trufor.pth.tar"
```
注意两个坑：① `test.py` 里 `except: pass` 会吞掉报错、跑完却输出空目录，先改成 `raise`；② 没 GPU 加 `-g -1`。

**2. 修了一个方向性 Bug（成员 1/3 区）**
之前 `before_after` 在「没输入」时默认返回"全部一致 + 归因 Strong"——也就是一行没算就断言"变美是产品的功劳"，跟我们"证据不足就 Unknown"的原则正好相反。
现已改成：无信号 → `Unknown`；有真实差异信号 → 才判 Weak/Strong。两个仓库都已改并跑通测试。

**3. 评测数字治理（成员 5 区）**
`benchmark_summary.json` 里那些 F1=0.78 之类的手写数字已清空，改成"目标值/待测"口径，明确标注无来源。评委一问就穿的假数字不会再出现在页面上。
（顺带确认：线上 demo 的「评测工作台」读的是成员 4 的真实结果，并显示诚实的"能力边界"表，不消费那个文件，所以不会崩。）

**4. C2PA 三态规整（成员 2 区）**
之前 C2PA 一律返回 `error`，太粗糙。现已区分 `absent`（无签名）/ `not_verified`（疑似有但无验证库）/ `unsupported_format`（格式不支持）/ `error`（读取异常）。签名验证本身仍待后续接 `c2pa-python`。

---

## 二、接下来各自接手

- **成员 2**：权重下完 → 用官方权重重做 TruFor 取证；⚠️ 记住 TruFor 的 `score` 是"越高越可疑"，接进我们系统时务必转成 `integrity_score = 1 - score`（我们字段是"越高越完整"，不转结论会整体判反）；再做困难负样本误报率报告。
- **成员 3**：把 100 对 FFHQ/FFHQR 数据集用起来——用经典 CV（MediaPipe 对齐 + 亮度/色温/高频能量比）把 before/after 算法从 mock 做实。**经典方法不用训练，所以 100 对全部能当评测集**，直接产出可对外说的检出率/误报率。⚠️ 数据里 `ground_truth_strength` 全是占位的 `medium`，别拿它当真值。
- **成员 4**：`full_pipeline_v1.py` 依赖的 `claim_extractor_mock_v2_1.py` 没随包上传，脚本现在跑不起来，补齐后让整个流水线能独立运行。
- **成员 5**：① 启动志愿者真实数据采集（W2 必须动，否则 W3 拿不出可信评测）；② 评测页只展示真实/目标值，别再放无来源数字。
- **我（成员 1）**：9/13 冻结接口 v2（只加 `mode`/`engine`/`provenance` 三个字段），随后做证据融合与报告生成。

## 三、时间线（不变）

**9/13 接口冻结 → 9/30 内部演示冻结 → 10/1–10/10 只做 PPT/视频/文档/演练，不写新功能。**

有两件事需要拍板：① 志愿者招募谁牵头（建议成员 5）；② 对外模型使用声明谁汇总（建议我来做：用了哪些第三方模型、什么许可、我们自己的贡献是什么）。

---
备注：旧版分工文档 v1–v4 均已作废，唯一有效版本是 `TrueGlow_五人分工与行动清单.md`。
